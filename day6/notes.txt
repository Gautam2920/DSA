1. Two Pointer?
 - tracking two indices.
 - one pointer - linear scan while two pointer - structure + constraint

 - Types:
  -Same direction :
  - Starts from same end.
  - Opposite direction :
   - Starts from both ends and moves inward.

 - Shrinking search space:
  - Moves indexes closer to reduce the space.

2. Prefix Sum? :
 - Compute once , store , dont compute again.
 - reduces time complexity
 - prefix[i] = prefix[i-1] + nums[i]

- Leetcode 1480:
 - simple prefix sum logic.

- Leetcode 167:
 - Two opp direction pointer left and right
 - currentSum = nums[left] + nums[right]
 - equal to target? return index +1
 - too big? right--
 - too small? left++ 

- Leetcode 209:
 - Use two pointers and sliding window.
 - track sum and minLength
 - add numbers to sum until sum >= target
 - check if smaller subarray is possible.

- Leetcode 560:
 - add element to sum
 - if sum - k exist in the map
 - count++
 - map sum - k
 - return count

- Leetcode 1493:
 - use sliding window
 - track zero count
 - for zeroCount > 1 , check left , if zero , left-- , update zeroCount
 - track maxLen as max(maxLen , right - left)